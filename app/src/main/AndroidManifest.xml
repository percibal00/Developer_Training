<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Developer_Training">
        <activity
            android:name="com.example.Vista.PerfilActivity"
            android:exported="false" />
        <activity
            android:name="com.example.Vista.HistorialActivity"
            android:exported="false" />
        <activity
            android:name="com.example.Vista.RegistroActivity"
            android:exported="false" />
        <activity
            android:name="com.example.Vista.MenuActivity"
            android:exported="false" />
        <activity
            android:name="com.example.Vista.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>